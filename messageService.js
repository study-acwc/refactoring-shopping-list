export function sendEmail(email, message) {
    /* 이메일 보내는 코드 */
    console.log("real sendEmail"); // 호출될 때 로그를 찍음
  }


  export class EMail {
    sendEmail2(email, message) {
        /* 문자를 보내는 코드 */
            console.log("real sendEmail2"); // 호출될 때 로그를 찍음
        }
}